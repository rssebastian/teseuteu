import React from 'react';

import LogoBox from '../LogoBox';

const Header = () => {
    return (
        <div className="header">
            <LogoBox />
        </div>
    );
};

export default Header;